/**
 * When {@link DayPickerProps.showWeekNumber} is set, the default ARIA label for header of the week
 * number column.
 */
export const labelWeekNumberHeader = (): string => {
  return 'Week Number';
};
